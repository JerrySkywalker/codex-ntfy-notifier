# Current Codex installation uses a Stop Hook, not the legacy `notify = [...]`
# setting. Run scripts/install-codex-ntfy.ps1: it enables [features].hooks and
# adds or upgrades only this product's owned Stop Hook in user-level hooks.json.
#
# Do not add a legacy notify declaration as well: that can create duplicate
# phone notifications. Existing unrelated notify declarations are preserved and
# reported by the installer for owner review.
